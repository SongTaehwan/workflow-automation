<!--
- 섹션 제목을 지우거나 바꾸지 마라. CI 가 제목으로 본문을 찾는다.
- 해당 없는 섹션에는 "없음" 이라고 적어라. 빈 섹션은 CI 가 차단한다.
- 한 PR = 한 논리적 변경. 리팩터링과 기능 추가를 섞지 마라.
-->

## 요약

<!-- 무엇을 왜 바꿨는지 2~4줄. diff 를 문장으로 옮겨 적지 마라. -->

## 작업 지시

<!--
이 작업을 지시한 이슈 링크. 이슈가 없으면 받은 요청 원문을 그대로 붙여라.
리뷰어가 "지시대로 했는지" 판별하는 기준이다.
Closes #
-->

## 검증

<!--
직접 실행해서 확인한 것만, 재현 절차를 단계로 적어라.
lint / typecheck / build 는 CI 가 돌린다. 여기 적지 마라.
확인하지 못한 게 있으면 무엇을 왜 확인 못 했는지 적어라.
-->

## 화면

<!-- UI 가 바뀌면 Vercel 프리뷰 URL 과 before / after. 아니면 "없음". -->

## 리뷰 포인트

<!--
확신 없는 판단, 세운 가정, 추측으로 채운 값, 검증하지 못한 동작,
요청 범위 밖 변경과 그 사유.
정말 없으면 "없음" 이라고 적어라. 빈칸은 CI 가 차단한다.
-->
