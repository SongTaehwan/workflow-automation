# workflow-automation

> **FSD 구조 규칙을 ESLint 로 정적으로 판정하고, 개발·커밋·푸시·CI·머지 단계에 중첩 배치한 harness 로 강제한다.**

이 한 줄이 이 저장소의 모든 규칙 문서와 도구 설정의 근거다. 규칙을 새로 만들거나 고칠 때 아래 순서로 판단한다.

- MUST: 정적으로 판정할 수 있는지 먼저 따진다. 가능하면 ESLint 룰로 만든다.
- MUST: 룰의 메시지에 "왜 + 대신 무엇을" 을 넣는다. 이 메시지가 문서를 대체한다.
- MUST: 룰로 만든 규칙은 문서에서 지운다. "lint 가 강제함" 포인터 줄도 남기지 않는다.
- MUST: 정적 판정이 불가능한 배치 판단만 문서에 남긴다.

## 문서 구성

- 레이어별 배치 규약: `src/<layer>/CLAUDE.md`
- 도구가 강제하는 규칙 전체 지도: `docs/rules/rules-map.md`
- MUST: 코드 배치를 결정하기 전에 해당 레이어의 `CLAUDE.md` 를 읽는다.

## 지침 문서 작성 규칙

대상: 이 저장소의 모든 `CLAUDE.md`.

- MUST: 라벨은 `MUST:` 하나만 쓴다.
- MUST: 금지는 본문을 부정문으로 끝낸다 (`~하지 않는다`).
- MUST: 금지에 목적지가 있으면 목적지를 지정하는 긍정 명령문으로 쓴다. 금지는 따라온다.
- MUST: 같은 규칙을 긍정문과 부정문으로 두 번 적지 않는다.

판별 근거: `MUST NOT:` 라벨에 긍정 본문(`~한다`)을 붙이면 본문만 읽었을 때 정반대 지시가 된다. 라벨과 본문의 극성을 일치시켜 부분 읽기에도 결론이 같도록 한다.

```
위반: - MUST NOT: 데이터 페칭을 라우트 정의에 넣는다.
      (본문만 읽으면 "넣어라" 로 읽힌다)

차선: - MUST: 데이터 페칭을 라우트 정의에 넣지 않는다.
      (극성 일치. 단 목표 상태를 알려주지 않는다)

정상: - MUST: 데이터 페칭은 entities/<slice>/api/ 에 둔다.
      (목적지 지정. 금지는 따라온다)
```

## PR 규칙

- MUST: PR 본문은 `.github/pull_request_template.md` 의 `##` 섹션 5개를 그대로 쓴다.
- MUST: 섹션 제목은 템플릿 그대로 둔다.
- MUST: 모든 섹션을 채운다. 해당 없으면 "없음" 이라고 적는다.
- MUST: 안내 주석은 지우고 내용을 채운다. 주석만 남기면 미작성으로 처리된다.
- MUST: 템플릿을 복사해 채운 파일을 `gh pr create --body-file` 로 넘긴다. `--body` 는 템플릿을 무시한다.
- MUST: "검증" 에는 직접 실행해 확인한 것만 재현 절차로 적는다. `lint`·`build` 결과는 적지 않는다. CI 가 돌린다.
- MUST: "리뷰 포인트" 에 확신 없는 판단, 세운 가정, 미검증 동작, 범위 밖 변경을 적는다. 비우지 않는다.
- MUST: 한 PR 에 한 논리적 변경만 담는다. 리팩터링과 기능 추가를 섞지 않는다.

`.github/workflows/pr-body.yml` 이 섹션 존재·작성 여부를, `.github/workflows/ci.yml` 이 lint·build 를 검사한다. 충돌 시 CI 결과가 우선한다.
