# push 대상 커밋이 없으면(HEAD 와 원격이 동일) lefthook 이 훅 전체를 건너뛴다.
# 검사할 변경이 없는 경우라 문제 없음. 우회가 필요하면 git push --no-verify.
pre-push:
  parallel: true
  commands:
    lint:
      run: pnpm lint
    test:
      run: pnpm test
    build:
      run: pnpm build
